# Test-only dependencies (not in the production image)
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.28.1
aiosqlite==0.20.0      # in-memory SQLite for tests (no real Postgres needed)
flake8==7.1.1
